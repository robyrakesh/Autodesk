{
  "name": "autdesk",
  "version": "1.0.0",
  "description": "e2e tests ",
  "main": "index.js",
  "scripts": {
    "cy:run": "cypress run --headless --browser chrome --env allure=true",
    "allure:report": "allure generate allure-results --clean && allure serve allure-results",
    "clean:allure": "rm -R -f allure-report/ && rm -R -f allure-results/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.11.0",
    "@faker-js/faker": "^7.6.0",
    "@shelex/cypress-allure-plugin": "^2.38.3"
  }
}
